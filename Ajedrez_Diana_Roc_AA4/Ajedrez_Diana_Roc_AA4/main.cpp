#include <iostream>
#include "constants.h"
#include "board.h"

int main() {
    // Initialize the board to the standard chess starting position.
    initBoard();

    // Print the current board to the console.
    printBoard();

    return 0;
}
