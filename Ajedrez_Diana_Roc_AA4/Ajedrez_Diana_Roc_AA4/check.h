#pragma once

// return true if the king is of the selected color is in check
bool isKingInCheck(bool whiteKing);

// return true if the selected color is in check mate
bool isCheckmate(bool whiteToMove);
